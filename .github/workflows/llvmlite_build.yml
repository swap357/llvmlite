name: llvmlite_win-64_wheel_builder

on:
  workflow_dispatch:
    inputs:
      llvmdev_run_id:
        description: 'llvmdev workflow run ID (optional)'
        required: false
        type: string

env:
  CONDA_CHANNEL: numba
  CONDA_LABEL: ci

jobs:
  win-64-build:
    name: win-64-build
    runs-on: windows-2019
    defaults:
      run:
        shell: bash -elx {0}
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
      fail-fast: false

    steps:
      - name: Clone repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          python-version: ${{ matrix.python-version }}
          channels: numba/label/win64_wheel,defaults,numba

      - name: Install build dependencies
        run: conda install llvmdev=15

      - name: Build wheel
        run: |
          conda activate build_env
          python setup.py bdist_wheel

      - name: Upload llvmlite wheel
        uses: actions/upload-artifact@v4
        with:
          name: llvmlite-win-64-py${{ matrix.python-version }}
          path: dist/*.whl
          compression-level: 0
          retention-days: 7
          if-no-files-found: error

  win-64-validate:
    name: win-64-validate
    needs: win-64-build
    runs-on: windows-2019
    defaults:
      run:
        shell: bash -elx {0}
    steps:
      - name: Setup Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          python-version: "3.12"

      - name: Install validation dependencies
        run: conda install py-lief wheel twine keyring rfc3986

      - name: Download llvmlite wheels
        uses: actions/download-artifact@v4
        with:
          pattern: llvmlite-win-64-*
          path: dist
          merge-multiple: true

      - name: Validate wheels
        run: |
          cd dist
          for WHL_FILE in *.whl; do
            wheel unpack $WHL_FILE
            python test_wheel.py llvmlite/binding/llvmlite.dll
            twine check $WHL_FILE
          done

  win-64-test:
    name: win-64-test
    needs: win-64-validate
    runs-on: windows-2019
    defaults:
      run:
        shell: bash -elx {0}
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
      fail-fast: false

    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Download llvmlite wheel
        uses: actions/download-artifact@v4
        with:
          name: llvmlite-win-64-py${{ matrix.python-version }}
          path: dist

      - name: Install and test
        run: |
          pip install dist/*.whl
          # Add the llvmlite DLL directory to PATH
          export PATH="$PATH:$(python -c 'import os,llvmlite; print(os.path.dirname(llvmlite.__file__) + "/binding")')"
          python -m llvmlite.tests
